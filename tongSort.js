function bucketSort (array, num) {
  if (array.length <=1) {
    return array
  }
  // 获取数组长度
  // 获取最小值
  // 获取最大值
  // 获取分组区块大小（最大容量）
  // ？
  var buckets = new Array(max - min + 1);
  // 循环遍历
  for (var i = 0; i < n; i++) {
    // 拿到每个数据
    // 得到桶的编号
    var index = Math.floor((el - min) / capacity);
    // 获取到对应索引的数值
    // 判断数值
  }
}